@REM Maven Wrapper
@REM This is a placeholder — run `mvn wrapper:wrapper` in the backend dir to generate the actual wrapper
@echo off
echo Please run 'mvn wrapper:wrapper' in the backend/ directory to generate the Maven wrapper.
